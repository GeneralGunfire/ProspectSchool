// Original audited career database — superseded by batch files in careers400Final.ts.
// Kept as an empty array; all careers now live in the batch_* files.
import type { CareerFull } from './careersTypes';

export const careerDatabase: CareerFull[] = [];
